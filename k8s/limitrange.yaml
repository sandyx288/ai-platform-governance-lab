apiVersion: v1
kind: LimitRange
metadata:
  name: team-limit
spec:
  limits:
  - default:
      cpu: "1"
      memory: "1Gi"
    defaultRequest:
      cpu: "500m"
      memory: "512Mi"
    type: Container
